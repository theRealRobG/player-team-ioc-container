{
  "name": "player-team-ioc-container",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm run build",
    "compile": "npm run cleanup && npm run lint && npm run build",
    "build": "tsc",
    "lint": "tslint -p . -c tslint.json 'src/**/*.ts'",
    "cleanup": "rm -rf ./lib"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^5.7.0"
  },
  "dependencies": {
    "typescript": "^2.5.3"
  }
}
